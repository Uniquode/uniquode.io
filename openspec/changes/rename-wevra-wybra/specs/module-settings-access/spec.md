## ADDED Requirements

### Requirement: Wybra module settings examples
Module settings documentation and examples SHALL use `wybra.*` namespaces for
package-owned module settings.

#### Scenario: Module settings examples name package modules
- **WHEN** module settings examples show package-owned settings access
- **THEN** they use `wybra.*` module and configuration namespaces
