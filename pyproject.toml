[project]
name = "uniquode"
version = "0.1.0"
description = "Local uv workspace for coordinated Uniquode and Wybra development"
requires-python = ">=3.13"
dependencies = [
    "app",
    "wybra",
]

[dependency-groups]
dev = [
    "httpx2>=2.3.0",
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
    "ruff>=0.15.16",
    "ty>=0.0.46",
]

[tool.uv]
package = false

[tool.uv.sources]
app = { workspace = true }
wybra = { workspace = true }

[tool.uv.workspace]
members = [
    "app",
    "wybra",
]

[tool.wybra]
runserver_reload_env = "APP_RELOAD"
runserver_app = "app.asgi:app"

[tool.ruff]
line-length = 88
src = [
    "app/src",
    "app/tests",
]
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
